# Copy to .env — already filled in with the shared hosted project's public
# values. The anon key is meant to be public (Postgres RLS is what actually
# gates the data), so it's safe to have it committed here.
EXPO_PUBLIC_SUPABASE_URL=https://monniczcdtpkuhuquwra.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1vbm5pY3pjZHRwa3VodXF1d3JhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODU4NTE1NDIsImV4cCI6MjEwMTQyNzU0Mn0.Q6VxZh_gCKhxhmo1PKO6FYg7R851GCoQEJEbjJU-Vm0

# Running against a local Supabase stack instead (npm run db:start prints
# these; anon key is the "anon key" line):
# EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Phone via Expo Go against the local stack? 127.0.0.1 is the phone itself —
# use your Mac's LAN IP (ipconfig getifaddr en0), e.g. http://192.168.1.20:54321
# Android emulator: http://10.0.2.2:54321
